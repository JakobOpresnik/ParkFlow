services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: parkflow
      POSTGRES_PASSWORD: parkflow
      POSTGRES_DB: parkflow
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/migrations:/docker-entrypoint-initdb.d:ro
    ports:
      - '5432:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U parkflow']
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    pull_policy: build
    command: bun run dev
    env_file:
      - ./backend/.env
    environment:
      # Password lives in PGPASSWORD, not in the URL: an inline user:pass@host reads
      # as a leaked credential to the secret scanners. `pg` picks PGPASSWORD up for
      # any field the connection string omits.
      DATABASE_URL: postgresql://parkflow@db:5432/parkflow
      PGPASSWORD: parkflow
      PORT: 3001
      # File-watch polling so `tsx watch` reloads on edits through the
      # Windows/Docker volume mount (mirrors the frontend service).
      CHOKIDAR_USEPOLLING: 'true'
      CHOKIDAR_INTERVAL: '300'
    volumes:
      - ./backend:/app
      - backend-node-modules:/app/node_modules
    ports:
      - '3001:3001'
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    pull_policy: build
    command: bun run dev --host 0.0.0.0 --port 3000
    environment:
      VITE_API_URL: http://localhost:3001
      VITE_OAUTH_AUTHORITY: https://sso.matheo.si/application/o/park-flow
      VITE_OAUTH_CLIENT_ID: V9RNWkqNeQq8FhnTCxnPr3hhlQLoBXHkp0k8k95j
      VITE_OAUTH_REDIRECT_URI: http://localhost:5173/callback
      VITE_OAUTH_ADMIN_GROUP: parkflow-admins
      CHOKIDAR_USEPOLLING: 'true'
      CHOKIDAR_INTERVAL: '300'
    volumes:
      - ./frontend:/app
      - frontend-node-modules:/app/node_modules
    ports:
      - '5173:3000'
    depends_on:
      - backend

volumes:
  postgres-data:
  backend-node-modules:
  frontend-node-modules:
